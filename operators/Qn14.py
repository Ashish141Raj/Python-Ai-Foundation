# 14.Take salary and increased by it by 10%

salary = float(input("Enter your salary: "))

increment = (salary*10)/100
net_salary = salary+increment

print(f"{salary} after increment 10% + {increment} = {net_salary}")